- Add [export to excel] and [import from excel] with a help navbar element that 
  describes what columns you need to import from (with example)

- Add "room" field to cat cards
  - based on the HTML template, it will determine where the cat is placed
    on the GUI

- Make a HTML template from [gui-07-17-2026.jpg] that will replace the
  unordered cards

- If a cat is not in a room, it should be at the bottom in an unordered card section

- "Yellow" should actually be named "Orange"
